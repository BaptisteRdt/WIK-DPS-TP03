version: '3.8'
services:
  app:
    ports:
      - '8080'

    build: 
      context: ./app_corr
      dockerfile: Dockerfile

    deploy:
      replicas: 4
    
    restart: always

    environment:
      - PORT=8080

  # proxy:
  #   image: nginx:latest
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
    
  #   ports:
  #     - 8081:80

  #   depends_on: 
  #     - app

